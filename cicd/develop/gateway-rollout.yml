apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: dev-gateway-rollout
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: dev-gateway
  template:
    metadata:
      labels:
        app: dev-gateway
    spec:
      containers:
        - name: dev-gateway
          image: simkids/dealivery-gateway:1.114
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: dev-gateway-service-configmap
  strategy:
    blueGreen:
      #activeService는 현재 운영중인 Blue 서비스
      activeService: dev-gateway-service-active

      #previewService는 새롭게 배포될 Green 서비스
      previewService: dev-gateway-service-preview
