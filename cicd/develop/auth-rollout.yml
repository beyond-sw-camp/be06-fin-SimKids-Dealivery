apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: dev-auth-rollout
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: dev-auth
  template:
    metadata:
      labels:
        app: dev-auth
    spec:
      containers:
        - name: dev-auth
          image: simkids/dealivery-auth:1.109
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: dev-auth-service-configmap
  strategy:
    blueGreen:
      #activeService는 현재 운영중인 Blue 서비스
      activeService: dev-auth-service-active

      #previewService는 새롭게 배포될 Green 서비스
      previewService: dev-auth-service-preview
